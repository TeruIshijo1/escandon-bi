require('./backend/node_modules/dotenv').config({path: './backend/.env'}); const sapService = require('./backend/services/sap.service'); async function fetchAllSapPages(sapService, initialUrl) { let allData = []; let nextLink = initialUrl; while (nextLink) { const res = await sapService.get(nextLink); if (res.data && res.data.value) { allData = allData.concat(res.data.value); } if (res.data && res.data['odata.nextLink']) { let link = res.data['odata.nextLink']; if (!link.startsWith('/')) link = '/' + link; nextLink = link; } else { nextLink = null; } } return allData; } fetchAllSapPages(sapService, '/SQLQueries(\'VidasSalvChoqueDet\')/List?startDate=\'2026-04-01\'').then(rows => { const counts={}; rows.forEach(row => { const f=row.FechaPrimeraOV; if(!f)return; const m=parseInt(f.substring(4,6), 10); counts[m]=(counts[m]||0)+1; }); console.log(counts); process.exit(0)}).catch(e=>{console.error(e);process.exit(1)});
